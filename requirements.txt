# Проверено на aiogram 3.31, Pillow 12.2, numpy 2.x, uharfbuzz 0.56.

aiogram>=3.15,<4
Pillow>=10.0,<13
python-dotenv>=1.0,<2

# Модель. Веса лежат в model/translit_model.npz, инференс — на NumPy,
# см. core/model_numpy.py. Раньше здесь стоял torch: pip ставил сборку с
# CUDA, и на ровном месте выходило больше пяти гигабайт (1.2 ГБ torch плюс
# 3.2 ГБ пакетов nvidia-*) ради сети на 428 тысяч параметров.
numpy>=1.24

# Раскладка текста для тодо бичиг: HarfBuzz выбирает позиционные формы букв
# (начальная/срединная/конечная), FreeType растеризует глифы. Без этого
# буквы рисуются изолированными и не соединяются — см. core/shaper.py.
uharfbuzz>=0.37
freetype-py>=2.3
